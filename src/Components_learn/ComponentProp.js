const ComponrntProp = (props) => {
  return (
    <div className="card">
      <h4>А у этого компонента есть свойство, например name = {props.name}</h4>
    </div>
  );
};

export default ComponrntProp;
